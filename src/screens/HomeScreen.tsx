import React, { useRef, useState, useEffect } from 'react'; import { StyleSheet, View, TouchableOpacity, Animated, PanResponder, useWindowDimensions, ScrollView, Platform, SafeAreaView, Modal, TextInput, Image, ActivityIndicator, FlatList, Pressable, Vibration, Alert } from 'react-native'; import { Container, Typography } from '../components/Base'; import { theme } from '../theme'; import { BrandLogo } from '../components/BrandLogo'; import { ThemeCard } from '../components/ThemeCard'; import { HistoricoModal } from '../components/HistoricoModal'; import { SynthesisActionCard, StateSurface } from '../components/ShellStateSurfaces'; import { Utensils, Zap, SlidersHorizontal, Activity, Database, Smartphone, X, User, Users, ChevronRight, ChevronDown, Menu, Battery, Heart, Scale, Droplets, Target, Settings, RefreshCw, Moon, Droplet, Brain, ChevronsDown, Sparkles, ArrowLeft, Calendar, History, Star, ChevronUp, Share, Dumbbell, Footprints } from 'lucide-react-native'; import Svg, { Path, Text as SvgText, TextPath, Defs, G } from 'react-native-svg'; import { BiomechanicRelic } from '../components/BiomechanicRelic'; import { SiderealBackground } from '../components/SiderealBackground'; import { GatingOverlay } from '../components/GatingOverlay'; // expo-linear-gradient and expo-blur: use require() guards to avoid web crash // v2.1 - web inline mini-app launch fix  // Web-safe LinearGradient fallback const LinearGradient = Platform.OS === 'web'   ? ({ style, colors, ...props }: any) => (     <View style={[style, { backgroundColor: colors?.[0] ?? 'rgba(0,0,0,0.8)' }]} {...props} />   )   : (() => { const { LinearGradient: LG } = require('expo-linear-gradient'); return LG; })();  // Web-safe BlurView fallback const BlurView = Platform.OS === 'web'   ? ({ style, ...props }: any) => (     <View style={[style, { backgroundColor: 'rgba(0,0,0,0.6)' }]} {...props} />   )   : (() => { const { BlurView: BV } = require('expo-blur'); return BV; })();  import { MINI_APP_CATALOG } from '../miniapps/catalog'; import { MiniAppContainer } from '../miniapps/MiniAppContainer'; import { MiniAppManifest } from '../miniapps/types'; import { useStore } from '../store/useStore'; import * as Selectors from '../store/selectors'; import { getSemanticInsights, getSemanticStatus, getAiStatus } from '../services/insights'; import { computeSemanticFromMeasurements } from '../services/semantic-output/analysis-engine'; import { AiInsight } from '../services/semantic-output/types'; import { useShallow } from 'zustand/react/shallow'; import { ENV } from '../config/env'; import { Analysis } from '../store/store'; import { aiInsightsService } from '../services/insights/ai-insights.service'; import { aiGatewayService } from '../services/ai-gateway/client'; import { semanticOutputService } from '../services/semantic-output'; import { MetricCard } from '../components/MetricCard'; import { getAllMetricsDefinitions, MetricObservation, MetricObservationStatus, MetricCategory } from '../data/metrics-catalog';  import { supabase } from '../services/supabase';   // --- SLOT MACHINE ODOMETER COMPONENT --- const SlotMachineOdometer = ({ targetNumber }: { targetNumber: number }) => {   const scrollAnim = useRef(new Animated.Value(0)).current;   const H = 34; // Height of each single digit frame   const NUM_POOL = 35; // Amount of numbers to scroll through    useEffect(() => {     // Reset to "0" instantly if it re-mounts or target changes     scrollAnim.setValue(0);     // Animate to the actual target over 2.5 seconds with a realistic ease-out     Animated.timing(scrollAnim, {       toValue: -H * targetNumber,       duration: 2500,       useNativeDriver: true,     }).start();   }, [targetNumber]);    // Array of 0, 1, 2, ... NUM_POOL   const numbers = Array.from({ length: NUM_POOL }, (_, i) => i);    return (     <View style={{ flexDirection: 'row', alignItems: 'flex-end', justifyContent: 'center' }}>        {/* Caleira interna completamente invisível & minimalista onde os números correm */}       <View style={{ height: H, overflow: 'hidden', justifyContent: 'flex-start', alignItems: 'center', minWidth: 26 }}>         <Animated.View style={{ transform: [{ translateY: scrollAnim }] }}>           {numbers.map((n) => (             <View key={n} style={{ height: H, justifyContent: 'center', alignItems: 'center' }}>               <Typography style={{ color: '#00F2FF', fontSize: 28, fontWeight: '800', letterSpacing: 0, textShadowColor: 'rgba(0, 242, 255, 0.4)', textShadowOffset: { width: 0, height: 0 }, textShadowRadius: 6 }}>                 {n}               </Typography>             </View>           ))}         </Animated.View>       </View>        <Typography style={{ color: 'rgba(255,255,255,0.95)', fontSize: 20, fontWeight: '800', letterSpacing: 2, textTransform: 'uppercase', marginLeft: 8, marginBottom: 2 }}>         dias       </Typography>     </View>   ); };  // --- MECHANICAL WHEEL PICKER COMPONENT --- const WheelPicker = ({ value, onChange, min = 1, max = 30, width = 80 }: { value: number, onChange: (v: number) => void, min?: number, max?: number, width?: number }) => {   const ITEM_HEIGHT = 34;   const numbers = Array.from({ length: max - min + 1 }, (_, i) => i + min);   const scrollViewRef = useRef<ScrollView>(null);    useEffect(() => {     const index = numbers.indexOf(value);     if (index >= 0 && scrollViewRef.current) {       scrollViewRef.current.scrollTo({ y: index * ITEM_HEIGHT, animated: true });     }   }, [value, numbers]);    return (     <View style={{ height: ITEM_HEIGHT * 3, overflow: 'hidden', alignItems: 'center', marginVertical: 0, width, alignSelf: 'center' }}>       {/* Indicador Central Cyberpunk */}       <View style={{ position: 'absolute', top: ITEM_HEIGHT, height: ITEM_HEIGHT, width: '100%', backgroundColor: 'rgba(0, 242, 255, 0.05)', borderTopWidth: 1, borderBottomWidth: 1, borderColor: 'rgba(0, 242, 255, 0.3)', zIndex: 0 }} pointerEvents="none" />        <ScrollView         ref={scrollViewRef}         showsVerticalScrollIndicator={false}         snapToInterval={ITEM_HEIGHT}         decelerationRate="fast"         onMomentumScrollEnd={(e) => {           const index = Math.round(e.nativeEvent.contentOffset.y / ITEM_HEIGHT);           if (numbers[index] !== undefined && numbers[index] !== value) {             onChange(numbers[index]);           }         }}         contentContainerStyle={{ paddingVertical: ITEM_HEIGHT }}       >         {numbers.map((n) => (           <TouchableOpacity             key={n}             activeOpacity={0.7}             onPress={() => onChange(n)}             style={{ height: ITEM_HEIGHT, justifyContent: 'center', alignItems: 'center', width: '100%' }}           >             <Typography style={{               fontSize: value === n ? 24 : 16,               color: value === n ? '#00F2FF' : 'rgba(255,255,255,0.2)',               fontWeight: value === n ? '800' : '500',               textShadowColor: value === n ? 'rgba(0, 242, 255, 0.5)' : 'transparent',               textShadowRadius: value === n ? 8 : 0             }}>               {n}             </Typography>           </TouchableOpacity>         ))}       </ScrollView>     </View>   ); };  export const HomeScreen = ({ navigation }: { navigation: any }) => {   const { width, height } = useWindowDimensions();   // Subscrição seletiva por domínio   const user = useStore(useShallow(Selectors.selectUser));   const activeMemberId = useStore(Selectors.selectActiveMemberId);   const credits = useStore(Selectors.selectCredits);   const measurements = useStore(useShallow(Selectors.selectMeasurements));   // [SAFE BUILD 1] Selectors dependentes de alocação de objetos instáveis desligados para diagnóstico de loop   // const exportedContexts = useStore(useShallow(Selectors.selectExportedContexts));   const exportedContexts: any[] = [];   const installedAppIds = useStore(useShallow((state) => state.installedAppIds || []));   const isGuestMode = useStore(state => state.isGuestMode);   const guestProfile = useStore(useShallow(state => state.guestProfile));   const isMeasuring = useStore(Selectors.selectIsMeasuring);   const isNfcLoading = useStore(Selectors.selectIsNfcLoading);   const hasResultsAccess = useStore(Selectors.selectHasResultsAccess);   // const aiConfidence = useStore(useShallow(state => Selectors.selectAiConfidence(state)));   const aiConfidence = {      level: 'limitada' as any,      score: 0,      factors: { positive: [], negative: [], info: [] },     label: 'SAFE MODE',     color: '#888',     recommendedAction: { label: 'Aguardar', intent: 'wait' }   };   // const dailySynthesis = useStore(useShallow(state => Selectors.selectDailySynthesis(state)));   const dailySynthesis = { status: 'needs_attention' as any, title: 'SAFE MODE', negativeHighlight: null, positiveHighlight: null, action: { label: 'Em Mitigação', intent: 'wait' as any } };    // Stable ID string — avoids re-firing effects when selectUser returns new obj ref   const userId = user?.id ?? null;    const setUser = useStore(state => state.setUser);   const setSessionToken = useStore(state => state.setSessionToken);   const setAnalyses = useStore(state => state.setAnalyses);   const setActiveAnalysisId = useStore(state => state.setActiveAnalysisId);   // SAFE BUILD: depend on stable userId string, NOT the user object,   // to prevent selectUser's .find() from returning a new ref every render.   useEffect(() => {     async function loadData() {       const session = await supabase.auth.getSession();       const token = session.data.session?.access_token;       const currentUser = useStore.getState().isGuestMode ? null : useStore.getState().user;              if (currentUser && token && !useStore.getState().isGuestMode) {         try {           const analysesRes = await fetch(`${ENV.BACKEND_URL}/analyses`, {             headers: { 'Authorization': `Bearer ${token}` }           });            if (analysesRes.ok) {             const realAnalyses = await analysesRes.json();             setAnalyses(realAnalyses);              const persistedId = currentUser?.activeAnalysisId;             const existsInReal = realAnalyses.some((a: any) => a.id === persistedId);              if (persistedId && existsInReal) {               setActiveAnalysisId(persistedId);             } else if (realAnalyses.length > 0) {               setActiveAnalysisId(realAnalyses[0].id);             } else {               setActiveAnalysisId(null);             }           }         } catch (err) {           console.error('[HomeScreen] Failed to load analyses:', err);         }       }     }      loadData();   // eslint-disable-next-line react-hooks/exhaustive-deps   }, [userId, isGuestMode]);    // Garantir inicialização do serviço sem dependência circular.   // Defer via setTimeout to prevent notify() chain from firing during mount render.   useEffect(() => {     const uid = userId || 'user_current_session_1';     const t = setTimeout(() => semanticOutputService.init(uid), 0);     return () => clearTimeout(t);   // eslint-disable-next-line react-hooks/exhaustive-deps   }, [userId]);    // Safe memoized facts query to avoid Zustand infinite render loop   const rawEvents = useStore(useShallow((state) => state.appContributionEvents));   const analyses = useStore(useShallow((state) => state.analyses));   const activeAnalysisId = useStore(state => state.activeAnalysisId);    // --- UI & NAVIGATION STATE ---   const [showDemoModal, setShowDemoModal] = useState(false);   // Demo uses the app-wide global demoAnalysis.   const demoAnalysis = useStore(useShallow(state => state.demoAnalysis));   const setDemoAnalysis = useStore(state => state.setDemoAnalysis);   const [showHistorico, setShowHistorico] = useState(false);   const [showExportModal, setShowExportModal] = useState(false);   const [bioTab, setBioTab] = useState(0);   const [themesOpen, setThemesOpen] = useState(false);   const [dataOpen, setDataOpen] = useState(false);   const [themesInteractive, setThemesInteractive] = useState(false);   const [dataInteractive, setDataInteractive] = useState(false);   const [expandedAppId, setExpandedAppId] = useState<string | null>(null);    // ── FONTE ÚNICA DE VERDADE ──────────────────────────────────────────────────────   // activeAnalysis: se Demo activo usa a análise temporária (source:'demo').   // Caso contrário usa a análise apontada por activeAnalysisId no store.   // Resultados + Leitura AI leem EXCLUSIVAMENTE daqui.   const activeAnalysis = React.useMemo<Analysis | null>(() => {     if (demoAnalysis) return demoAnalysis;          let baseAnalysis: Analysis | null = null;     if (activeAnalysisId) {       baseAnalysis = analyses.find(a => a.id === activeAnalysisId) ?? null;     }     if (!baseAnalysis) {       baseAnalysis = analyses[0] ?? null;     }      if (!baseAnalysis) return null;      // Only create a new object if there are exportedContexts to inject.     // Otherwise return baseAnalysis directly to preserve referential stability.     if (exportedContexts.length === 0) return baseAnalysis;      const mappedContexts = exportedContexts.map(ctx => {       let valStr = String(ctx.value);       if (typeof ctx.value === 'object' && ctx.value !== null) {         valStr = ctx.value.label || ctx.value.status || JSON.stringify(ctx.value);       }       return {         id: ctx.id,         type: `${ctx.category} | ${ctx.key.toUpperCase()}`,         sourceAppId: ctx.appId,         value: valStr,         timestamp: new Date(ctx.updatedAt).getTime(),         memberId: ctx.memberId       } as any;     });      return {       ...baseAnalysis,       ecosystemFacts: [         ...(baseAnalysis.ecosystemFacts || []),         ...mappedContexts       ]     };   }, [demoAnalysis, activeAnalysisId, analyses, exportedContexts]);    // ── PREV ANALYSIS PARA TENDÊNCIAS ───────────────────────────────────────────────   const prevAnalysis = React.useMemo(() => {     const isDemo = activeAnalysis?.source === 'demo';     if (isDemo || !activeMemberId || !activeAnalysis) return null;     return analyses.find(a =>        a.memberId === activeMemberId &&        a.source !== 'demo' &&        a.id !== activeAnalysis.id &&        new Date(a.createdAt).getTime() < new Date(activeAnalysis.createdAt).getTime()     ) || null;   }, [activeAnalysis, activeMemberId, analyses]);    const prevSemanticState = React.useMemo(() => {     if (!prevAnalysis || prevAnalysis.measurements.length === 0) return null;     return computeSemanticFromMeasurements(prevAnalysis.measurements, prevAnalysis.ecosystemFacts || []);   }, [prevAnalysis]);    const getAugmentedSemanticInsights = React.useCallback(() => {     let base = getSemanticInsights();     if (!prevSemanticState) {        return base.map(t => ({ ...t, trend: 'no_base' as const }));     }     return base.map(theme => {        const oldDomain = prevSemanticState.domains[theme.domain];        let trendVal: 'improving' | 'worsening' | 'stable' | 'no_base' = 'no_base';        let priorityVal: 'noise' | 'discrete' | 'relevant' | 'critical' = 'noise';         if (oldDomain && typeof theme.score === 'number' && typeof oldDomain.score === 'number') {           const diff = theme.score - oldDomain.score;           const absDiff = Math.abs(diff);            if (absDiff < 2) {              trendVal = 'stable';              priorityVal = 'noise';           } else {              trendVal = diff > 0 ? 'improving' : 'worsening';              if (absDiff < 5) priorityVal = 'discrete';              else if (absDiff < 15) priorityVal = 'relevant';              else priorityVal = 'critical';           }        }        return { ...theme, trend: trendVal, priority: priorityVal };     });   }, [prevSemanticState]);    // Leitura dos biomarcadores da análise activa (formaáto normalizado)   const filteredMeasurements = React.useMemo(() => {     if (!activeAnalysis) return [];     // Converte AnalysisMeasurement para o formato legado esperado pela UI     return activeAnalysis.measurements.map(m => ({       type: m.type,       timestamp: m.recordedAt,       value: { marker: m.marker, value: m.value, unit: m.unit },     }));   }, [activeAnalysis]);    // Sinais de ecossistema da análise activa   const activeFacts = React.useMemo(() => {     return activeAnalysis?.ecosystemFacts ?? [];   }, [activeAnalysis]);    // selectedDate — derivado da análise activa (para compat. com componentes que o esperam)   const selectedDate = activeAnalysis?.analysisDate ?? null;    // Settings Form State (Grupos de Análise)   const [selectedGroups, setSelectedGroups] = useState<string[]>(['U', 'S', 'F', 'O']);   const [groupsExpanded, setGroupsExpanded] = useState(false);   const [showOutrosWarning, setShowOutrosWarning] = useState(false);    const handleToggleGroup = (id: string) => {     if (selectedGroups.includes(id)) {       setSelectedGroups(selectedGroups.filter(g => g !== id));     } else {       setSelectedGroups([...selectedGroups, id]);       if (id === 'O') {         setShowOutrosWarning(true);       }     }   };    const factualBioCategories = React.useMemo(() => {     const src = activeAnalysis?.measurements ?? [];     const ecoSrc = activeAnalysis?.ecosystemFacts ?? [];      const urinalysisMarkers = src       .filter(m => m.type === 'urinalysis')       .map(m => ({ name: m.marker || 'Análise Urinária', value: m.value, unit: m.unit }));      const physiologyMarkers = src       .filter(m => ['ecg', 'ppg', 'temp', 'weight'].includes(m.type))       .map(m => ({ name: m.marker || m.type, value: m.value, unit: m.unit }));      const fecalMarkers = src       .filter(m => m.type === 'fecal')       .map(m => ({ name: m.marker || 'Marcador Fecal', value: m.value, unit: m.unit }));      return [       { label: 'Análises de Urina',          color: '#00F2FF', markers: urinalysisMarkers, id: 'U', shortLabel: 'Urina' },       { label: 'Monitorização Fisiológica', color: '#00D4AA', markers: physiologyMarkers, id: 'S', shortLabel: 'Fisiológica' },       { label: 'Avaliação Fecal',           color: '#FFA500', markers: fecalMarkers,       id: 'F', shortLabel: 'Fecal' },       {         label: 'Sinais do Ecossistema', color: '#FFD700', id: 'E', shortLabel: 'Ecossistema',         markers: ecoSrc.map(f => ({           name: String(f.type).replace(/_/g, ' ').toUpperCase(),           value: f.value,           unit: f.sourceAppId,         }))       },     ].filter(c => c.id === 'E' || selectedGroups.includes(c.id));   }, [activeAnalysis, selectedGroups]);    // Ações via subscrição estática (sem re-render por estado)   const launchApp = useStore(state => state.launchApp);   const uninstallApp = useStore(state => state.uninstallApp);    // Subscrição ao Bundle Semântico v1.2.0 (Fonte de Verdade)   const [semanticThemes, setSemanticThemes] = useState(getSemanticInsights());   const [semanticStatus, setSemanticStatus] = useState(getSemanticStatus());   const [crossDomainSummary, setCrossDomainSummary] = useState(semanticOutputService.getCrossDomainSummary());      // ── ESTADO DA IA (v1.3.0) ──   const [aiState, setAiState] = useState(getAiStatus());    // ── FONTE ÚNICA DE VERDADE ──────────────────────────────────────────────────────   // activeAnalysis muda → loadAnalysis injeta bundle calculado → SemanticOutputStore   // notifica subscritores → setSemanticThemes/setSemanticStatus actualiza a UI.   // Resultados e Leitura AI leem do mesmo objecto — zero fontes paralelas.   // [SAFE BUILD 2] Desativada a cadeia reativa de Semantic Engine que provocava re-renders circulares   /*   useEffect(() => {     semanticOutputService.loadAnalysis(activeAnalysis);   }, [activeAnalysis]);    useEffect(() => {     const unsubscribe = semanticOutputService.subscribe(() => {       setSemanticThemes(getSemanticInsights());       setSemanticStatus(getSemanticStatus());       setAiState(getAiStatus());       setCrossDomainSummary(semanticOutputService.getCrossDomainSummary());     });     return unsubscribe;   }, []);   */    const themesFlatListRef = useRef<FlatList>(null);   const themesPanelHeight = height;   const [showProfile, setShowProfile] = useState(false);   const [showControl, setShowControl] = useState(false);   const [showTokens, setShowTokens] = useState(false);   const [showNfcModal, setShowNfcModal] = useState(false);   const [isNfcScanning, setIsNfcScanning] = useState(false);   // ── Animation States ────────────────────────────────────────────────────   const themesAnim = useRef(new Animated.Value(-width)).current;   const dataAnim = useRef(new Animated.Value(width)).current;   const pulseAnim = useRef(new Animated.Value(1)).current;   const floatAnim1 = useRef(new Animated.Value(0)).current;   const floatAnim2 = useRef(new Animated.Value(1)).current;   const arrowAnim = useRef(new Animated.Value(0)).current;    // ── DEMO MODE ────────────────────────────────────────────────────────────────   // Demo cria análise temporária em memória (source:'demo').   // NÃO é adicionada a analyses[] — não contamina o histórico real.   // Sair do demo: setDemoAnalysis(null) — volta ao activeAnalysisId.   const handleSelectDemo = (key: any) => {     setShowDemoModal(false);      // ── Desativar Demo (key === null) ──────────────────────────────────────     if (!key) {       setDemoAnalysis(null);       // Fecha ambos os painéis limpo       setDataOpen(false);       setThemesOpen(false);       Animated.spring(dataAnim, { toValue: width, bounciness: 0, useNativeDriver: false }).start(() => setDataInteractive(false));       Animated.spring(themesAnim, { toValue: -width, bounciness: 0, useNativeDriver: false }).start(() => setThemesInteractive(false));       return;     }      // ── Activar cenário Demo ───────────────────────────────────────────────     // 1. Fecha Resultados (estado imediato — não depende de animação terminar)     setDataOpen(false);     Animated.spring(dataAnim, { toValue: width, bounciness: 0, useNativeDriver: false }).start(() => setDataInteractive(false));      // 2. Cria baseline factual DEMO e injeta no objecto análise temporária completa     let demoMeasurements = [       { id: 'd_u1', type: 'urinalysis', marker: 'Hidratação', value: 1.015, unit: 'SG', recordedAt: Date.now() },       { id: 'd_u2', type: 'urinalysis', marker: 'pH', value: 6.5, unit: '', recordedAt: Date.now() },       { id: 'd_u3', type: 'urinalysis', marker: 'Oxidação (MDA)', value: 1.2, unit: 'µmol', recordedAt: Date.now() },       { id: 'd_u4', type: 'urinalysis', marker: 'Leucócitos', value: 0, unit: 'cel/uL', recordedAt: Date.now() },       { id: 'd_f1', type: 'fecal', marker: 'Bristol', value: 4, unit: 'Tipo', recordedAt: Date.now() },       { id: 'd_f2', type: 'fecal', metricKey: 'bowel_regularity', marker: 'Regularidade intestinal', value: 'Regular', unit: '', recordedAt: Date.now() },       { id: 'd_f3', type: 'fecal', metricKey: 'fecal_characterization', marker: 'Caracterização', value: 'Sem anomalias visuais', unit: '', recordedAt: Date.now() },       { id: 'd_ecg', type: 'ecg', marker: 'Ritmo Sinusal', value: 65, unit: 'bpm', recordedAt: Date.now() },       { id: 'd_p1', type: 'ppg', marker: 'VFC (HRV)', value: 55, unit: 'ms', recordedAt: Date.now() },       { id: 'd_t', type: 'temp', marker: 'Variação T. Basal', value: 0.1, unit: 'C', recordedAt: Date.now() },       { id: 'd_w', type: 'weight', marker: 'Estabilidade', value: 72.0, unit: 'kg', recordedAt: Date.now() }     ];      let demoEcoFacts = [       { id: 'f_sleep', type: 'sono_profundo', value: '1h45', sourceAppId: 'sleep_ring', recordedAt: Date.now() },       { id: 'f_steps', type: 'passos_diarios', value: '8500', sourceAppId: 'health_os', recordedAt: Date.now() }     ];      // Overrides específicos por cenário DEMO     if (key === 'low_energy') {       demoMeasurements = demoMeasurements.map(m =>         m.marker === 'VFC (HRV)' ? { ...m, value: 32 } :         m.marker === 'Ritmo Sinusal' ? { ...m, value: 78 } :         m.marker === 'Hidratação' ? { ...m, value: 1.025 } :         m.marker === 'Oxidação (MDA)' ? { ...m, value: 3.8 } : m       );       demoEcoFacts = demoEcoFacts.map(f => f.type === 'sono_profundo' ? { ...f, value: '0h50' } : f);     } else if (key === 'poor_recovery') {       demoMeasurements = demoMeasurements.map(m =>         m.marker === 'VFC (HRV)' ? { ...m, value: 25 } :         m.marker === 'Variação T. Basal' ? { ...m, value: 0.6 } :         m.marker === 'Ritmo Sinusal' ? { ...m, value: 84 } : m       );       demoEcoFacts = demoEcoFacts.map(f => f.type === 'sono_profundo' ? { ...f, value: '0h35' } : f);     } else if (key === 'irregular_digestion') {       demoMeasurements = demoMeasurements.map(m =>         m.marker === 'Bristol' ? { ...m, value: 6 } :         m.marker === 'Regularidade intestinal' ? { ...m, value: 'Irregular' } :         m.marker === 'Caracterização' ? { ...m, value: 'Fezes desfeitas, margens irregulares' } :         m.marker === 'pH' ? { ...m, value: 8.0 } : m       );     } else if (key === 'mixed' || key === 'mixed_profile') {       demoMeasurements = demoMeasurements.map(m =>         m.marker === 'Bristol' ? { ...m, value: 2 } :         m.marker === 'VFC (HRV)' ? { ...m, value: 40 } :         m.marker === 'Hidratação' ? { ...m, value: 1.020 } : m       );     } // "balanced" não tem overrides porque a baseline já é equilibrada      const demo = {       id: `demo_${Date.now()}`,       source: 'demo',       demoScenarioKey: key,       analysisDate: new Date().toISOString().split('T')[0],       deviceSources: ['demo_system'],       ecosystemFacts: demoEcoFacts.map((f: any) => ({ ...f, value: String(f.value ?? '') })),       measurements: demoMeasurements.map((m: any) => ({ ...m, value: String(m.value ?? '') }))     } as any;     setDemoAnalysis(demo);       // 3. Forçar fecho do painel Leitura AI (Themes) para todos os utilizadores.     // Evita o bug de "ecrã lavado/branco" provocado pelo overlap de animações vs Modals na Web.     setThemesOpen(false);     Animated.spring(themesAnim, { toValue: -width, bounciness: 0, useNativeDriver: false }).start(() => setThemesInteractive(false));   };    const handleExitDemo = () => {     setDemoAnalysis(null);   };    const [stableExpanded, setStableExpanded] = useState(false);   const [profileName, setProfileName] = useState(user?.name || 'Utilizadora');   const [profileAge, setProfileAge] = useState(user?.age ? user.age.toString() : '');   const [profileWeight, setProfileWeight] = useState(user?.weight ? user.weight.toString() : '');   const [profileHeight, setProfileHeight] = useState(user?.height ? user.height.toString() : '');    // A. Sync local profile state when profile modal opens.   // Use userId (stable string) NOT the user object — user object gets a new reference   // when setUser(fallbackProfile) is called during boot, which would re-fire this effect   // on every render and cause the React #185 setState cascade.   useEffect(() => {     if (showProfile) {       if (isGuestMode) {         const gp = useStore.getState().guestProfile;         setProfileName(gp?.name || 'Convidada');         setProfileAge(gp?.age ? gp.age.toString() : '');         setProfileWeight(gp?.weight ? gp.weight.toString() : '');         setProfileHeight(gp?.height ? gp.height.toString() : '');       } else {         const u = useStore.getState().user;         const newVal = u?.name || 'Utilizador';         if ((window as any) && (window as any)._lastSavedName && newVal !== (window as any)._lastSavedName) {            console.warn(`[DEV NAME 6] rehydration overwrite detected: saved "${(window as any)._lastSavedName}" but store injected "${newVal}"`);         }         setProfileName(newVal);         setProfileAge(u?.age ? u.age.toString() : '');         setProfileWeight(u?.weight ? u.weight.toString() : '');         setProfileHeight(u?.height ? u.height.toString() : '');       }     }   // eslint-disable-next-line react-hooks/exhaustive-deps   }, [showProfile, isGuestMode, userId]);   // NOTE: userId (string) replaces user (object) to prevent new-ref cascade on fallback inject    // Odometer calculation (Factual)   // selectDataFreshness returns a new object with Date.now() on every call.   // Subscribing to it via useShallow caused permanent re-renders.   // Fix: subscribe only to the measurement count (stable scalar) and compute freshness in useMemo.   const measurementCount = useStore(state => (state.measurements || []).length);   const lastMeasurementTs = useStore(state => {     const m = state.measurements || [];     if (m.length === 0) return 0;     return Math.max(...m.map(x => new Date(x.timestamp || 0).getTime() || 0));   });   const dataFreshness = React.useMemo(() =>     Selectors.selectDataFreshness(useStore.getState()),   // eslint-disable-next-line react-hooks/exhaustive-deps   [measurementCount, lastMeasurementTs]);   const diasSemExame = dataFreshness.daysSince ?? 0;    // Settings Form State (Modo de Análise)   const [syncFlowState, setSyncFlowState] = useState<'idle'|'searching'|'connected'|'syncing'|'success'|'failed'>('idle');    const executeSyncReal = () => {      setSyncFlowState('searching');      // Anúncio global de sync para bloquear outras gavetas se necessário      useStore.getState().setIsMeasuring(true);            // 1. Procurar      setTimeout(() => {         setSyncFlowState('connected');         // 2. Conectado         setTimeout(() => {            setSyncFlowState('syncing');            // 3. Recolha Mútua            setTimeout(() => {                // SUCCESS - injetar mock measurement na camada canonica                useStore.getState().setIsMeasuring(false);                setSyncFlowState('success');                                const storeRef = useStore.getState();                // Resgate tatico: se foi barrado por restricao antes mas acionamos o sync forçado pela view pessoal?                // Wait, activeMemberId always tells who we're focusing on.                const targetId = storeRef.activeMemberId || storeRef.user?.id;                                const payloadArray = [                  { id: 'scan_' + Date.now(), type: 'vitals', source: 'nfc_scanner', timestamp: new Date().toISOString(), memberId: targetId, value: { marker: 'Energia Base', value: 94 } },                  { id: 'scan_c_' + Date.now(), type: 'vitals', source: 'nfc_scanner', timestamp: new Date().toISOString(), memberId: targetId, value: { marker: 'Cardio Físico', value: 68 } }                ];                 useStore.setState({ measurements: [...payloadArray, ...(storeRef.measurements || [])] });                 setTimeout(() => {                    setSyncFlowState('idle');                }, 3000);            }, 2500);         }, 1600);      }, 1800);   };    // Escuta ativa de pedidos de Sincronização vindos do Agregado ou outras tabs   // SAFE BUILD: guard prevents re-entry loop — executeSyncReal calls setIsMeasuring(true)   // which would re-fire this effect without the guard.   const isMeasuringRef = useRef(false);   useEffect(() => {      if (isMeasuring && syncFlowState === 'idle' && !isMeasuringRef.current) {         isMeasuringRef.current = true;         executeSyncReal();      }      if (!isMeasuring) {         isMeasuringRef.current = false;      }   }, [isMeasuring, syncFlowState]);    const [analysisMode, setAnalysisMode] = useState<'manual' | 'automatico'>('automatico');   const [autoTimes, setAutoTimes] = useState(1);   const [autoDays, setAutoDays] = useState(1);   const [autoExpanded, setAutoExpanded] = useState(false);   const [showAutoWarning, setShowAutoWarning] = useState(false);     type NotificationMode = 'Ativas' | 'Apenas Alertas' | 'Silenciadas';   const [notificationMode, setNotificationMode] = useState<NotificationMode>('Ativas');    const cycleNotificationMode = () => {     if (notificationMode === 'Ativas') setNotificationMode('Apenas Alertas');     else if (notificationMode === 'Apenas Alertas') setNotificationMode('Silenciadas');     else setNotificationMode('Ativas');   };    // ── Inline mini-app for web (same pattern as AppsScreen) ─────────────────   const [inlineApp, setInlineApp] = useState<MiniAppManifest | null>(null);    const arrowTranslate = arrowAnim.interpolate({     inputRange: [0, 1],     outputRange: [-5, 10]   });    const arrowOpacity = arrowAnim.interpolate({     inputRange: [0, 0.7, 1],     outputRange: [0, 1, 0]   });    // HOTFIX CRUCIAL: Iniciar e Forçar o loop de animação das setas (estava cego sem isto!)   React.useEffect(() => {     let isActive = true;     const runAnimation = () => {       if (!isActive) return;        arrowAnim.setValue(0); // Reinicia obrigatoriamente para 0        Animated.timing(arrowAnim, {         toValue: 1,         duration: 1500, // Ciclo urgente de 1.5 seg         useNativeDriver: false, // Forçando JS Thread obriga bypass à placa gráfica         isInteraction: false, // Para não ser pausado por toques do utilizador       }).start(() => {         if (isActive) {           // Quebra a Call Stack síncrona para que o RN Web não ignore o reinício           setTimeout(runAnimation, 20);         }       });     };      runAnimation();      return () => {       isActive = false;       arrowAnim.stopAnimation();     };   }, [arrowAnim]);    // Backdrop darkening: fades to black when Temas or Dados panels slide open   const themesBackdropOpacity = themesAnim.interpolate({     inputRange: [-width, 0],     outputRange: [0, 0.88],     extrapolate: 'clamp',   });   const dataBackdropOpacity = dataAnim.interpolate({     inputRange: [0, width],     outputRange: [0.88, 0],     extrapolate: 'clamp',   });    // ── Home screen "transport" effect: shifts sideways as panels open ─────────   // When Temas (left panel) opens, home moves right. When Dados opens, home moves left.   const homeShiftFromThemes = themesAnim.interpolate({     inputRange: [-width, 0],     outputRange: [0, width],     extrapolate: 'clamp',   });   const homeShiftFromData = dataAnim.interpolate({     inputRange: [0, width],     outputRange: [-width, 0],     extrapolate: 'clamp',   });   const homeShiftX = Animated.add(homeShiftFromThemes, homeShiftFromData);    // Slight scale-down effect to add depth feel (1 → 0.88 as panel opens)   const homeShrinkFromThemes = themesAnim.interpolate({     inputRange: [-width, 0],     outputRange: [1, 0.88],     extrapolate: 'clamp',   });   const homeShrinkFromData = dataAnim.interpolate({     inputRange: [0, width],     outputRange: [0.88, 1],     extrapolate: 'clamp',   });   const homeShrink = Animated.multiply(homeShrinkFromThemes, homeShrinkFromData);    // Mutable refs for edge gesture callbacks (avoid stale closures in PanResponder)   const openThemesRef = useRef<() => void>(() => { });   const openDataRef = useRef<() => void>(() => { });    // ── Panel open/close helpers (sync animation + state for web backdrop) ────   const openThemes = () => {     if (dataOpen) return; // prevent overlap: don't open if Dados is open     setThemesInteractive(true);     setThemesOpen(true);     Animated.spring(themesAnim, { toValue: 0, bounciness: 0, useNativeDriver: false }).start();   };   const closeThemes = () => {     setThemesOpen(false);     Animated.spring(themesAnim, { toValue: -width, bounciness: 0, useNativeDriver: false }).start(() => {       setThemesInteractive(false);     });   };   const openData = () => {     if (themesOpen) return; // prevent overlap: don't open if Temas is open     setDataInteractive(true);     setDataOpen(true);     Animated.spring(dataAnim, { toValue: 0, bounciness: 0, useNativeDriver: false }).start();   };   const closeData = () => {     setDataInteractive(false);     if (themesOpen) closeThemes();     Animated.spring(dataAnim, {       toValue: width,       bounciness: 0,       speed: 14,       useNativeDriver: false,     }).start(() => {       setDataOpen(false);     });   };    // Keep edge gesture callbacks up to date every render   openThemesRef.current = openThemes;   openDataRef.current = openData;    const handleSynthesisAction = (intent: string) => {     if (intent === 'sync_now' || intent === 're_sync') {       closeData(); closeThemes();       setTimeout(() => executeSyncReal(), 400);     } else if (intent === 'manage_permissions' || intent === 'complete_profile') {       closeData(); closeThemes(); setShowProfile(true);     } else if (intent === 'explore_analysis') {       closeData(); openThemes();     } else if (intent === 'open_timeline') {       closeData(); closeThemes(); setShowHistorico(true);     } else if (intent === 'open_context') {       alert('Aceda ao App Place na shell principal para autorizar integrações.');     }   };    // ── Switch Setup ──────────────────────────────────────────────────────────   const switchAnim = useRef(new Animated.Value(0)).current; // 0 = UP, 160 = DOWN   const isOff = useRef(false);    const switchPanResponder = useRef(     PanResponder.create({       onStartShouldSetPanResponder: () => true,       onStartShouldSetPanResponderCapture: () => true,       onMoveShouldSetPanResponder: () => true,       onMoveShouldSetPanResponderCapture: () => true,       onPanResponderTerminationRequest: () => false,       onPanResponderGrant: () => {         switchAnim.stopAnimation();       },       onPanResponderMove: (_, gestureState) => {         // Divide dy by 0.6 because the visual component is scaled to 60%         let newY = (isOff.current ? 160 : 0) + (gestureState.dy / 0.6);         if (newY < 0) newY = 0;         if (newY > 160) newY = 160;         switchAnim.setValue(newY);       },       onPanResponderRelease: (_, gestureState) => {         let newY = (isOff.current ? 160 : 0) + (gestureState.dy / 0.6);         let toValue = 0;         // Se passar da métrica ou tiver pull rápido para baixo         if (newY > 60 || (gestureState.vy / 0.6) > 0.4) {           toValue = 160;           isOff.current = true;         } else {           toValue = 0;           isOff.current = false;         }         Animated.spring(switchAnim, {           toValue,           useNativeDriver: Platform.OS !== 'web',           bounciness: 4,           speed: 14,         }).start(({ finished }) => {           if (finished && toValue === 160) {             setShowNfcModal(true);           }         });       }     })   ).current;    // Left edge gesture zone (Temas) - captures swipe right from left 60px   const leftEdgeGesture = useRef(PanResponder.create({     onStartShouldSetPanResponder: () => Platform.OS === 'web',     onMoveShouldSetPanResponder: () => Platform.OS === 'web',     onPanResponderRelease: (_, { dx, dy }) => {       if (dx > 50 && Math.abs(dy) < 120) openThemesRef.current();     },   })).current;    // Right edge gesture zone (Dados) - captures swipe left from right 60px   const rightEdgeGesture = useRef(PanResponder.create({     onStartShouldSetPanResponder: () => Platform.OS === 'web',     onMoveShouldSetPanResponder: () => Platform.OS === 'web',     onPanResponderRelease: (_, { dx, dy }) => {       if (dx < -50 && Math.abs(dy) < 120) openDataRef.current();     },   })).current;    const DRAWER_DOWN = 583;   const DRAWER_UP = 0;   const lastDrawerY = useRef(DRAWER_DOWN);   const drawerAnim = useRef(new Animated.Value(DRAWER_DOWN)).current;    // Bottom edge gesture zone (App Place drawer) - swipe up from bottom to open   const bottomEdgeGesture = useRef(PanResponder.create({     onStartShouldSetPanResponder: () => Platform.OS === 'web',     onMoveShouldSetPanResponder: () => Platform.OS === 'web',     onPanResponderRelease: (_, { dy, vy }) => {       if (dy < -30 || vy < -0.3) {         Animated.spring(drawerAnim, { toValue: 0, bounciness: 0, useNativeDriver: false })           .start(() => { lastDrawerY.current = 0; });       }     },   })).current;    // Force sync drawer position on hot reload so user sees exact bottom state   React.useEffect(() => {     drawerAnim.setValue(DRAWER_DOWN);     lastDrawerY.current = DRAWER_DOWN;   }, [DRAWER_DOWN]);    const drawerBgOpacity = drawerAnim.interpolate({     inputRange: [DRAWER_UP, DRAWER_DOWN],     outputRange: [1, 0.10],     extrapolate: 'clamp',   });    const drawerInnerOpacity = drawerAnim.interpolate({     inputRange: [DRAWER_UP, DRAWER_DOWN],     outputRange: [1, 0.50], // Elevado 15% a pedido para mais legibilidade     extrapolate: 'clamp',   });    const centerContentY = drawerAnim.interpolate({     inputRange: [DRAWER_UP, DRAWER_DOWN],     outputRange: [-230, -80],     extrapolate: 'clamp',   });    const homeBlurBackdropOpacity = drawerAnim.interpolate({     inputRange: [DRAWER_UP, DRAWER_DOWN],     outputRange: [1, 0], // Fully visible when UP (drawer open), invisible when DOWN     extrapolate: 'clamp',   });    const drawerPanResponder = useRef(     PanResponder.create({       onStartShouldSetPanResponder: () => true,       onMoveShouldSetPanResponder: () => true,       onPanResponderMove: (_, { dy }) => {         let newY = lastDrawerY.current + dy;         if (newY < DRAWER_UP) newY = DRAWER_UP;         if (newY > DRAWER_DOWN) newY = DRAWER_DOWN;         drawerAnim.setValue(newY);       },       onPanResponderRelease: (_, { dy, vy }) => {         let finalY = lastDrawerY.current + dy;         let toValue = DRAWER_DOWN;                  if (vy < -0.2 || dy < -25 || (lastDrawerY.current === DRAWER_DOWN && dy < -15)) {           toValue = DRAWER_UP; // Fast swipe up or short deliberate swipe up         } else if (vy > 0.2 || dy > 25 || (lastDrawerY.current === DRAWER_UP && dy > 15)) {           toValue = DRAWER_DOWN; // Fast swipe down or short deliberate swipe down         } else if (finalY < (DRAWER_DOWN + DRAWER_UP) / 2) {           toValue = DRAWER_UP;         } else {           toValue = DRAWER_DOWN;         }          Animated.spring(drawerAnim, {           toValue,           bounciness: 0,           useNativeDriver: false,         }).start(() => {           lastDrawerY.current = toValue;         });       }     })   ).current;    // ── Central Visual Animation ──────────────────────────────────────────────   // [LOOP FIX] This effect was removed — it conflicted with the arrowAnim loop at line 684.   // Both used the same Animated.Values (arrowAnim, pulseAnim, floatAnim1, floatAnim2)   // but this one used useNativeDriver:true while the drawer uses useNativeDriver:false.   // Mixing native/non-native drivers on the same Animated.Value caused internal   // React state updates on web that manifested as React #185 (maximum update depth).   // The pulse/float animations below are re-enabled safely with useNativeDriver:false.   React.useEffect(() => {     Animated.loop(       Animated.sequence([         Animated.timing(pulseAnim, { toValue: 1.2, duration: 3000, useNativeDriver: false }),         Animated.timing(pulseAnim, { toValue: 1, duration: 3000, useNativeDriver: false }),       ])     ).start();      Animated.loop(       Animated.sequence([         Animated.timing(floatAnim1, { toValue: 1, duration: 15000, useNativeDriver: false }),         Animated.timing(floatAnim1, { toValue: 0, duration: 15000, useNativeDriver: false }),       ])     ).start();      Animated.loop(       Animated.sequence([         Animated.timing(floatAnim2, { toValue: 0, duration: 20000, useNativeDriver: false }),         Animated.timing(floatAnim2, { toValue: 1, duration: 20000, useNativeDriver: false }),       ])     ).start();   }, []);    // ── Gesture Handlers ──────────────────────────────────────────────────────   const mainPanResponder = useRef(     PanResponder.create({       onStartShouldSetPanResponder: () => false,       onMoveShouldSetPanResponder: (_, { dx, dy, y0 }) => {         // Se a gaveta estiver aberta, protegemos o scroll horizontal das apps         if (lastDrawerY.current < 200 && y0 > 250 && Math.abs(dx) > Math.abs(dy)) {           return false;         }          // Capture se for um arrastão longo (lateral para Menus ou para cima na zona inferior)         if (Math.abs(dx) > 30) return true;         if (Math.abs(dy) > 20 && y0 > 300) return true;         return false;       },       onPanResponderMove: (_, { dx, dy, y0 }) => {         // Se o gesto começar na metade inferior e for maioritariamente vertical, liga ao arrasto da App Place         if (y0 > 300 && Math.abs(dy) > Math.abs(dx)) {           let newY = lastDrawerY.current + dy;           if (newY < DRAWER_UP) newY = DRAWER_UP;           if (newY > DRAWER_DOWN) newY = DRAWER_DOWN;           drawerAnim.setValue(newY);         }       },       onPanResponderRelease: (_, { x0, dx, dy, vy, y0 }) => {         const isDrawerOpen = lastDrawerY.current < 200;          // Left Edge Swipe -> Themes (blocked if drawer is open)         if (!isDrawerOpen && x0 < 120 && dx > 50) {           openThemesRef.current();           return;         }         // Right Edge Swipe -> Data (blocked if drawer is open)         if (!isDrawerOpen && x0 > width - 120 && dx < -50) {           openDataRef.current();           return;         }          // Vertical Swipe (Bottom Half) -> App Drawer         if (y0 > 300 && Math.abs(dy) > Math.abs(dx)) {           if (dy < -25 || vy < -0.2) {             Animated.spring(drawerAnim, { toValue: DRAWER_UP, useNativeDriver: false }).start(() => lastDrawerY.current = DRAWER_UP);           } else if (dy > 25 || vy > 0.2) {             Animated.spring(drawerAnim, { toValue: DRAWER_DOWN, useNativeDriver: false }).start(() => lastDrawerY.current = DRAWER_DOWN);           } else {             // Revert             Animated.spring(drawerAnim, { toValue: lastDrawerY.current, useNativeDriver: false }).start();           }         }       },     })   ).current;    // ── Web touch gesture detector (mobile browser) ─────────────────────────   useEffect(() => {     if (Platform.OS !== 'web') return;     let touchStartX = 0;     let touchStartY = 0;     const onTouchStart = (e: TouchEvent) => {       touchStartX = e.touches[0].clientX;       touchStartY = e.touches[0].clientY;     };     const onTouchEnd = (e: TouchEvent) => {       // Se a gaveta estiver aberta, ignoramos gestos laterais (protege o carrocel das apps)       if (lastDrawerY.current < 200) return;        const dx = e.changedTouches[0].clientX - touchStartX;       const dy = e.changedTouches[0].clientY - touchStartY;       const screenW = window.innerWidth;       // Left edge swipe right → open Temas (via ref for mutual exclusion)       if (touchStartX < 120 && dx > 50 && Math.abs(dy) < 80) {         openThemesRef.current();       }       // Right edge swipe left → open Dados (via ref for mutual exclusion)       if (touchStartX > screenW - 120 && dx < -50 && Math.abs(dy) < 80) {         openDataRef.current();       }     };     window.addEventListener('touchstart', onTouchStart, { passive: true });     window.addEventListener('touchend', onTouchEnd, { passive: true });     return () => {       window.removeEventListener('touchstart', onTouchStart);       window.removeEventListener('touchend', onTouchEnd);     };   }, []);    // ── Drawer handle DOM touch events (for reliable mobile web drag) ─────────   const drawerHandleRef = useRef<any>(null);   useEffect(() => {     if (Platform.OS !== 'web') return;     const el = drawerHandleRef.current;     if (!el) return;     let startY = 0;     let startDrawerY = 0;     const onTStart = (e: TouchEvent) => {       startY = e.touches[0].clientY;       startDrawerY = lastDrawerY.current;     };     const onTMove = (e: TouchEvent) => {       const dy = e.touches[0].clientY - startY;       // Only track as drag if movement is significant (>20px prevents jitter)       if (Math.abs(dy) > 20) {         const newY = Math.max(0, Math.min(DRAWER_DOWN, startDrawerY + dy));         drawerAnim.setValue(newY);       }     };     const onTEnd = (e: TouchEvent) => {       const dy = e.changedTouches[0].clientY - startY;       // Only snap if it was a real drag (>20px) — otherwise let onPress fire normally       if (Math.abs(dy) > 20) {         const finalY = startDrawerY + dy;         const toValue = finalY < DRAWER_DOWN / 2 ? DRAWER_UP : DRAWER_DOWN;         Animated.spring(drawerAnim, { toValue, bounciness: 0, useNativeDriver: false })           .start(() => { lastDrawerY.current = toValue; });       }     };     el.addEventListener('touchstart', onTStart, { passive: true });     el.addEventListener('touchmove', onTMove, { passive: true });     el.addEventListener('touchend', onTEnd, { passive: true });     return () => {       el.removeEventListener('touchstart', onTStart);       el.removeEventListener('touchmove', onTMove);       el.removeEventListener('touchend', onTEnd);     };   }, []);    // (isDraggingDrawer removed — movement threshold in onTEnd handles drag vs tap)    const handleOpenMiniApp = (appId: string) => {     console.log('[HomeScreen] Intentando abrir MiniApp:', appId);     // Vibration.vibrate([0, 10, 5, 10]); // Padrão de vibração se quiseres manter      const app = MINI_APP_CATALOG.find((a: any) => a.id === appId);     if (app) {       try {         // Tentativa de navegação forçada para o topo         navigation.navigate('MiniApp', {           app         });       } catch (err: any) {         console.error('[HomeScreen] Erro na navegação:', err);       }     } else {       console.warn('[HomeScreen] MiniApp não encontrada:', appId);     }   };    // ── Web: render mini-app inline (iframe via MiniAppContainer.web.tsx) ─────   if (Platform.OS === 'web' && inlineApp) {     return (       <MiniAppContainer         app={inlineApp}         navigation={{ goBack: () => setInlineApp(null) }}       />   // STEP 15: SKELETON ONLY   return (     <View style={{ flex: 1, backgroundColor: '#0a0014', justifyContent: 'center', alignItems: 'center', padding: 24 }}>       <View style={{ backgroundColor: '#005500', padding: 16, borderRadius: 12, marginBottom: 12, width: '100%', alignItems: 'center' }}>         <Typography style={{ color: 'white', fontWeight: 'bold', fontSize: 16 }}>STEP 15 � Skeleton ONLY</Typography>         <Typography style={{ color: 'rgba(255,255,255,0.8)', fontSize: 11, marginTop: 4 }}>Validates if loop is in Container or Hooks</Typography>       </View>     </View>   ); };  const styles = StyleSheet.create({});